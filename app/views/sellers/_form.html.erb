<% was_validated = seller.errors.any? %>

<%= form_with(model: seller, local: true, novalidate: true, class: "mb-3") do |f| %>
  <%= f.button class: "btn btn-outline-secondary btn-block" %>
<% end %>
